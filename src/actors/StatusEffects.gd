extends Node
# When does poisoning happen?
# The first turn after it has been applied.
# What is a turn?
# The first move an actor takes when it has enough energy. and then every 1000 ticks after.
# So monster gets 1000 ticks